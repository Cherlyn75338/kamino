[package]
name = "scope_pyth_pull_poc"
version = "0.1.0"
edition = "2021"

[dependencies]
# Minimal dependencies to demonstrate the freshness issue
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]